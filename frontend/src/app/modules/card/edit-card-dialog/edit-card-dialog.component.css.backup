:host ::ng-deep .mat-dialog-container {
  padding: 0;
  border-radius: 16px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.08);
  max-width: 95vw;
}

.dialog-form-container {
  padding: 2rem 2.5rem 1.5rem 2.5rem;
  width: 100%;
  min-width: 260px;
  max-width: 700px;
  box-sizing: border-box;
}

@media (max-width: 1200px) {
  .dialog-form-container {
    max-width: 500px;
    padding: 1.5rem 1rem;
  }
}

@media (max-width: 900px) {
  .dialog-form-container {
    max-width: 95vw;
    padding: 1.25rem 0.5rem;
  }
}

/* Opcional: haz que los botones estén siempre abajo y el form tenga separación */
form {
  display: flex;
  flex-direction: column;
  gap: 1.3rem;
}

mat-form-field {
  width: 100%;
}

.error-message {
  color: #b00020;
  display: flex;
  align-items: center;
  gap: 0.4rem;
  font-size: 0.95rem;
  margin-bottom: 0.7rem;
}

[mat-dialog-actions] {
  justify-content: flex-end;
  gap: 0.7rem;
  margin-top: 0.5rem;
}